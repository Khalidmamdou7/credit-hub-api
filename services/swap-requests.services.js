const { getDriver } = require('../neo4j/neo4j');

const getSwapRequests = async (user) => {
}

const createSwapRequest = async (user, timeslot) => {
}

const updateSwapRequest = async (user, timeslotId, timeslot) => {
}

const deleteSwapRequest = async (user, timeslotId) => {
}


module.exports = {
    getSwapRequests,
    createSwapRequest,
    updateSwapRequest,
    deleteSwapRequest
};